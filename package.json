{
  "name": "eleventy-dir-data-bug",
  "version": "1.0.0",
  "description": "Reduced test case for a potential Eleventy bug",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "build-broken": "eleventy --config=eleventy-abs-paths.js",
    "start": "eleventy --serve",
    "start-broken": "eleventy --config=eleventy-abs-paths.js --serve"
  },
  "dependencies": {},
  "devDependencies": {
    "@11ty/eleventy": "0.10.0"
  },
  "author": "James Nash (http://cirrus.twiddles.com/)",
  "license": "ISC"
}
